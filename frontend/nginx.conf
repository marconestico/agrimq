server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA, le rotte le gestisce il client
    location / {
        try_files $uri $uri/ /index.html;
    }

    # "backend" è il nome del servizio in compose
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";

        # /api/stream è SSE: niente buffering e connessione lunga
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }
}
